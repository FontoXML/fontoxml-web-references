define(
	[
		'fontoxml-references-web'
	],
	function (
		uiReferencesHyperlinks
		) {
		'use strict';

		describe('fontoxml-references-web', function () {
			it('can be loaded', function () {
			});
		});
	}
);
